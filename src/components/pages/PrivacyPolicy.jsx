import React from "react";
import { withRouter } from "react-router-dom";

const PrivacyPolicy = () => {
  return (
    <div>
      This web application is a side project. I am learning to encrypt your data
      and I will update the page when it has been accomplished. As of January
      28, 2020.
    </div>
  );
};

export default withRouter(PrivacyPolicy);
